language: julia
os:
  - linux
julia:
  - release
notifications:
  email: false
script:
  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
  - julia --check-bounds=yes -e 'Pkg.clone(pwd()); Pkg.clone("https://github.com/JuliaPOMDP/POMDPs.jl"); Pkg.clone("https://github.com/JuliaPOMDP/POMDPModels.jl"); Pkg.clone("https://github.com/JuliaPOMDP/POMDPToolbox.jl"); Pkg.clone("https://github.com/JuliaPOMDP/POMDPDistributions.jl"); Pkg.clone("https://github.com/JuliaPOMDP/GenerativeModels.jl"); Pkg.test("DiscreteValueIteration"; coverage=true)'
after_success:
  - julia -e 'cd(Pkg.dir("DiscreteValueIteration")); Pkg.add("Coverage"); using Coverage; Coveralls.submit(Coveralls.process_folder())'
